<%
scrape_configs = [
{
  "job_name" => "service_metrics_exporter_#{spec.name}",
  "scheme" => "https",
  "tls_config" => {
    "server_name" => p('service_metrics.tls.server_name'),
  },
  "dns_sd_configs" => [
    {
      "names" => ["q-s0.#{spec.name}.*.*.#{spec.dns_domain_name}"],
      "type" => "A",
      "port" => p('service_metrics.port'),
    },
  ]
}
]
%>

<%= YAML.dump({"scrape_configs" => scrape_configs}) %>